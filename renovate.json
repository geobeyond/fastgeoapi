{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "description": [
    "Every GitHub Action is referenced by commit digest with the version in a comment. Renovate already updated digests that were pinned by hand; the preset is what pins an action added later, so the policy holds without anyone remembering it."
  ],
  "packageRules": [
    {
      "matchPackageNames": ["typer"],
      "allowedVersions": "<0.16",
      "description": "Typer 0.17+ breaks safety CLI (AttributeError: module 'typer' has no attribute 'rich_utils'). Waiting for safety update."
    }
  ]
}
